// ---------------------------------------------------------------------------
// Рамки, в которых сайт показывает загруженные картинки.
//
// Сайт вписывает фото в рамку заданной пропорции и обрезает всё, что не
// поместилось (object-cover). Если пропорции картинки и рамки расходятся,
// теряются края: широкий снимок экрана в рамке 4:3 лишается боков.
//
// Размеры взяты из вёрстки сайта, а не придуманы:
//   направления, продукты группы, проекты, профиль компании — aspect-[4/3];
//   приборы — карточка каталога h-60 (от 5:4 до 3:2 в зависимости от
//   ширины экрана), страница решения 4:3, окно прибора — во всю колонку.
// Для приборов 4:3 — лучший компромисс: срез по краям минимальный.
// ---------------------------------------------------------------------------

// Размер рамки на сайте посчитан по вёрстке для двух типичных экранов:
// 1920 px (обычный монитор) и 1440 px (ноутбук). Контент там шириной
// 1504 и 1248 px — контейнер Tailwind минус отступы по краям.
//
// Загружать нужно вдвое крупнее рамки: современные экраны показывают
// по два пикселя на один, и фото ровно в размер рамки выглядит мыльным.
// 1600 × 1200 — это не меньше двух рамок во всех разделах сразу, поэтому
// размер для загрузки везде один.
export const FRAMES = {
  default: {
    width: 1600,
    height: 1200,
    site: [],
    where: '',
  },
  products: {
    width: 1600,
    height: 1200,
    site: [
      { screen: 1920, width: 352, height: 240 },
      { screen: 1440, width: 288, height: 240 },
    ],
    where: 'Карточка каталога. На странице решения рамка 352 × 264 px, в окне прибора — крупнее. '
      + 'Рамка на разных экранах разной пропорции, поэтому держите прибор в центре, с запасом по краям.',
  },
  projects: {
    width: 1600,
    height: 1200,
    site: [
      { screen: 1920, width: 485, height: 364 },
      { screen: 1440, width: 400, height: 300 },
    ],
    where: 'Карточка проекта. По клику фото открывается целиком — поэтому и нужен крупный файл.',
  },
  directions: {
    width: 1600,
    height: 1200,
    site: [
      { screen: 1920, width: 637, height: 477 },
      { screen: 1440, width: 525, height: 394 },
    ],
    where: 'Справа на странице направления.',
  },
  portfolio: {
    width: 1600,
    height: 1200,
    site: [
      { screen: 1920, width: 613, height: 460 },
      { screen: 1440, width: 507, height: 380 },
    ],
    where: 'На странице продукта. По клику открывается целиком.',
  },
  banners: {
    width: 1920,
    height: 1080,
    site: [
      { screen: 1920, width: 1920, height: 1080 },
      { screen: 1440, width: 1440, height: 810 },
    ],
    where: 'Баннер на весь первый экран. На телефоне показывается вертикальная середина кадра, поэтому главное держите ближе к центру.',
  },
  company: {
    width: 1600,
    height: 1200,
    site: [
      { screen: 1920, width: 670, height: 503 },
      { screen: 1440, width: 549, height: 411 },
    ],
    where: 'На страницах разделов «О компании».',
  },
};

// Человеческое название пропорции: 16:9 понятнее, чем 1.78.
const KNOWN = [
  [1, '1:1'], [5 / 4, '5:4'], [4 / 3, '4:3'], [3 / 2, '3:2'], [16 / 10, '16:10'],
  [16 / 9, '16:9'], [2, '2:1'], [21 / 9, '21:9'], [4 / 5, '4:5'], [3 / 4, '3:4'],
  [2 / 3, '2:3'], [9 / 16, '9:16'],
];

export function ratioLabel(width, height) {
  if (!width || !height) return '';
  const ratio = width / height;
  const [value, label] = KNOWN.reduce((best, item) => (
    Math.abs(item[0] - ratio) < Math.abs(best[0] - ratio) ? item : best
  ));
  return Math.abs(value - ratio) / value < 0.03 ? label : ratio.toFixed(2).replace('.', ',') + ':1';
}

// Разбор загруженной картинки против рамки. Возвращает null, если всё
// в порядке, или текст предупреждения, если края обрежутся или фото мелкое.
export function checkAgainstFrame(width, height, frame = FRAMES.default) {
  if (!width || !height) return null;
  const target = frame.width / frame.height;
  const ratio = width / height;
  const notes = [];

  const diff = Math.abs(ratio - target) / target;
  if (diff > 0.08) {
    // Какая доля картинки уйдёт за край рамки.
    const lost = ratio > target ? 1 - target / ratio : 1 - ratio / target;
    const side = ratio > target ? 'слева и справа' : 'сверху и снизу';
    notes.push(
      `пропорции ${ratioLabel(width, height)}, а рамка ${ratioLabel(frame.width, frame.height)} — `
      + `обрежется около ${Math.round(lost * 100)}% ${side}`,
    );
  }

  if (width < frame.width / 2) {
    notes.push(`картинка маленькая, ${width} px по ширине — на большом экране будет нечёткой`);
  }

  return notes.length ? notes.join('; ') : null;
}
